const Cat = require('../models/cat');

const createCat = ({ name, color }) => {
}

const getCatById = (catId) => {
}

const getCatsList = () => {
}

const updateCat = (catId) => {
}

const deleteCat = (catId) => {
}

module.exports = {
  getCatById,
  getCatsList,
  updateCat,
  deleteCat,
}
